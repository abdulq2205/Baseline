# Optional. Defaults to file:./prisma/dev.db when unset (see prisma.config.ts).
DATABASE_URL="file:./prisma/dev.db"

# Shown on the dashboard and the printed report. One org per deployment.
ORG_NAME="Your organization"
